import React from "react";

function TimerControls({startTimer, pauseTimer, resetTimer}) {

    return (
        <div className="timer-controls">
            <button id="star-stop" onClick={startTimer} >Start</button>
            <button id="start-stop" on
        </div>
    )
}